{
  "name": "neoninja",
  "workspaces": [
    "packages/base-ui",
    "packages/demo"
  ],
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run dev --workspaces",
    "clean": "npm run clean --workspaces"
  }
}
